import {options} from '../Interface/BotOptionsInterface';

/**
 * Define configuration options
 */
export const ENV_VAR: options = {
    identity: {
        username: 'yourBotName',
        password: 'yourToken',
    },
    channels: [
        'yourChannel'
    ]
}